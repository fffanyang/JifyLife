# 2026-03-14

## 首页输入框核心入口重构

- 重构了 `InputBar` 组件（`apps/web/src/components/InputBar/InputBar.tsx`）：
  - 新增 `variant` prop 支持 `hero`（首页居中大输入框）和 `docked`（底部固定栏）两种模式
  - 将 `<input>` 改为 `<textarea>`，支持多行输入和自动高度调整
  - 修复了语音识别的闭包陷阱（使用 `transcriptRef` 追踪最新 transcript）
  - 新增 `autoFocus` prop

- 重构了 `MainView` 首页（`apps/web/src/pages/MainView.tsx`）：
  - 输入框置于页面顶部居中作为核心入口（hero 模式）
  - 添加了时间段问候语（早上好/下午好等）
  - 添加了 AI 智能助理标签提示
  - 添加了 5 种条目类型的快捷提示标签（日程/备忘/流水/打卡/知识）
  - 添加了今日统计概览（按类型显示条目数量）
  - 移除了底部固定 InputBar，改为页面内滚动布局
  - 时间线展示在输入框下方

## JifyLife 纯前端 PWA 改造

将 JifyLife 从前后端分离架构改造为**纯前端 PWA**，不再需要任何服务器，数据存在手机浏览器本地 IndexedDB 中。

### 新增文件
- `apps/web/src/services/db.ts` — 基于 Dexie.js 的 IndexedDB 数据库定义
- `apps/web/src/services/localEntryService.ts` — 本地数据 CRUD + 基于规则的智能分类
- `apps/web/src/stores/entryStore.ts` — Zustand entry store，管理条目状态并同步 IndexedDB

### 改造的文件
- `apps/web/src/pages/MainView.tsx` — Mock 数据 → IndexedDB 读写
- `apps/web/src/App.tsx` — 移除 React Query、登录路由
- `apps/web/src/components/TopBar/TopBar.tsx` — 移除认证相关逻辑
- `apps/web/vite.config.ts` — 移除 API proxy，增强 PWA 离线缓存
- `apps/web/src/services/api.ts` — 弃用（原 axios 封装）
- `apps/web/src/services/authService.ts` — 弃用
- `apps/web/src/stores/authStore.ts` — 简化为常驻认证

### 依赖变化
- 新增：`dexie`（IndexedDB 封装）、`uuid`（本地 ID 生成）
- 移除：`axios`、`@tanstack/react-query`

### 部署方式
构建产物为纯静态文件（~416KB），可部署到任何静态托管：GitHub Pages、Vercel、Netlify、EdgeOne Pages 等。手机打开网址即可使用，支持 PWA 添加到桌面。

## 全面重构：马伯庸式人生日记

用户要求重新调整需求，将 JifyLife 彻底改造为"马伯庸式人生日记"——一个极简本地日记 PWA。

### 架构
- 纯前端 PWA，数据存 IndexedDB（Dexie.js），不需要任何后端/服务器
- React + Vite + TailwindCSS v4 + react-router-dom + zustand

### 清理的旧文件
- 全部旧组件（InputBar, TopBar, Timeline, TimelineCard, BottomSheet, SidePanel）
- 全部旧服务（api.ts, authService.ts, entryService.ts, localEntryService.ts）
- 全部旧 store（authStore.ts, entryStore.ts, settingsStore.ts）
- 旧页面（LoginPage.tsx, MainView.tsx）
- 旧 hook（useMediaQuery.ts）
- 移除依赖：@jifylife/shared, uuid, @types/uuid

### 新建的文件
- `src/types/diary.ts` — 日记数据模型 + 字段/天气常量
- `src/services/db.ts` — Dexie.js IndexedDB 定义（MaBoyongDiary 库）
- `src/services/diaryService.ts` — 完整 CRUD + Markdown 生成 + 统计 + 导出
- `src/stores/diaryStore.ts` — Zustand 日记状态管理
- `src/pages/HomePage.tsx` — 首页（日记列表 + 今日快捷入口）
- `src/pages/EditPage.tsx` — 编辑页（日期 + 天气 + 6项输入 + 图片 + 保存）
- `src/pages/DiaryDetailPage.tsx` — 日记详情页（马伯庸风格排版）
- `src/pages/ReviewPage.tsx` — 回顾页（周/月/年统计卡片）
- `src/pages/SettingsPage.tsx` — 设置页（存储路径 + 导出 + 版本）

### 4 个主页面
1. 编辑页：日期前后翻页 + 天气选择 + 事项/阅读/输入/输出/生活 5 个文本框 + 作息（起床/睡觉时间选择器）+ 图片（1-9张）+ 保存
2. 日记详情页：分模块排版展示，图片加载失败显示"图片已删除"
3. 回顾页：周/月/年切换 + 作息/事务/生活高频统计卡片 + 一句话总结 + 品牌水印（适合截图分享）
4. 设置页：存储路径 + 复制 + 导出全部日记（.md + index.json）+ 清除数据 + 版本信息

### 构建结果
- 构建产物：CSS 21KB + JS 303KB（gzip 97KB），PWA precache 11 entries (~792KB)
- 生成了 512x512 和 192x192 PWA 图标

## 设计升级：参考小红书极简J人管理系统

用户参考小红书笔记「3个APP构建的个人管理系统，极简J人极度舒适」（丁米娅），对应用进行全面设计升级。

### 设计理念变更
- 借鉴：三模块系统化管理（记录+任务+时间），极简分类框架，低门槛随时可用
- 配色从冷灰调 → 温暖文人棕调（#2C2520 主色，#8B7355 强调色，#F8F6F3 背景）
- 字体栈增加 LXGW WenKai / Noto Serif SC 优先（文人气质）
- 卡片系统统一为 `.card` / `.share-card` 等可复用样式类
- 新增玻璃态顶栏 `.glass-header`、分割线 `.divider`、标签胶囊 `.pill`
- 新增多种动画：slideUp / fadeIn / scaleIn

### 首页重设计
- 新增**日历视图**（月视图，周一起始，标记有日记的日期）
- 新增**选中日期概览面板**（模块标签 + 摘要 + 作息）
- 下方显示最近 5 条日记预览卡片
- 日记卡片右侧增加日期数字装饰

### 编辑页升级
- 文本输入从 `<input>` 改为 `<textarea>`，支持自适应高度（auto-resize）
- 每个字段独立 `FieldCard` 组件，带入场延迟动画
- 天气选择弹窗使用 scaleIn 动画
- 底部保存栏使用 glass-header 效果

### 详情页优化
- 完全使用 `share-card` 截图分享风格
- 标题区显示"月日"格式 + 年份装饰
- 模块标题使用 accent 色
- 作息数据使用大字号展示
- 底部水印：「人生日记 · 日期」

### 回顾页强化
- 统计卡片增加小 icon 装饰
- 事务模块使用 `CountCard` 彩色数字展示
- 生活高频标签使用胶囊样式
- 分区之间使用渐变分割线
- 总结使用中文书名号引用格式

### 设置页更新
- 版本号更新为 v2.0.0
- 每个设置区块增加 icon 装饰圆角方块
- 灵感来源补充"极简J人管理系统"

### 构建结果
- CSS 21.65KB + JS 310.87KB（gzip 98.53KB），PWA precache 11 entries (800KB)
- 零 lint 错误

## 分类体系重构

用户反馈"输入""输出"分类太抽象，不直觉。重新调整为行动导向的五分类：

| 旧字段 | 新字段 | 中文标签 | 图标 |
|--------|--------|----------|------|
| tasks | work | 工作 | 💼 |
| reading | study | 学习 | 📖 |
| input | fitness | 运动健康 | 💪 |
| output | expense | 生活消费 | 🛒 |
| life | mood | 心情感悟 | 💭 |

### 改动范围
- `types/diary.ts` — DiaryEntry 接口、TaskStats 接口、DIARY_FIELDS 常量
- `services/db.ts` — 新增 Dexie v2 迁移，自动将旧数据字段名映射到新字段
- `services/diaryService.ts` — Markdown 导出、统计计算、总结生成
- `pages/EditPage.tsx` — 编辑表单状态变量和 fieldSetters/fieldValues
- `pages/DiaryDetailPage.tsx` — sections 数组
- `pages/HomePage.tsx` — 模块指示器、摘要、日记卡片
- `pages/ReviewPage.tsx` — CountCard 标签，grid 改为 3 列 5 项

### 同时修复
- 编辑页保存后 800ms 自动返回首页（之前缺少确认反馈）

### 构建结果
- CSS 21.65KB + JS 311.58KB（gzip 98.79KB），零错误

## GitHub Pages 部署配置

配置项目支持通过 GitHub Pages 部署为纯静态 PWA 站点。

### 修改的文件
- `apps/web/vite.config.ts` — 添加动态 `base` 路径（通过 `VITE_BASE_PATH` 环境变量），PWA manifest 的 `start_url` 和 `scope` 改为相对路径 `"."`，图标路径改为相对路径
- `apps/web/src/App.tsx` — `BrowserRouter` 改为 `HashRouter`（GitHub Pages 不支持 SPA 路由重写）
- `README.md` — 添加 GitHub Pages 部署步骤说明

### 新增文件
- `.github/workflows/deploy.yml` — GitHub Actions 自动部署工作流：checkout → pnpm install → build（带 VITE_BASE_PATH）→ upload-pages-artifact → deploy-pages

### 部署步骤
1. 创建 GitHub 仓库并推送代码
2. Settings → Pages → Source 选 "GitHub Actions"
3. 推送到 main 分支自动部署
4. 访问 `https://<username>.github.io/JifyLife/`
5. 手机浏览器打开后"添加到主屏幕"安装为 PWA

### 构建验证
- 构建成功，所有资源路径正确带上 `/JifyLife/` 前缀
- manifest.webmanifest 使用相对路径，PWA 安装不受 base path 影响
